{
    "builds": [
        {
            "src": "frams/wsgi.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "15mb",
                "runtime": "python3.9",
                "buildCommand": "apt-get update && apt-get install -y cmake && pip install -r requirements.txt"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "frams/wsgi.py"
        }
    ]
}